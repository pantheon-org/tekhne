- Reviewing skill...

Validation Checks

  ✔ skill_md_line_count - SKILL.md line count is 109 (<= 500)
  ✔ frontmatter_valid - YAML frontmatter is valid
  ✔ name_field - 'name' field is valid: 'conventional-commits'
  ✔ description_field - 'description' field is valid (537 chars)
  ✔ compatibility_field - 'compatibility' field not present (optional)
  ✔ allowed_tools_field - 'allowed-tools' field not present (optional)
  ✔ metadata_version - 'metadata' field not present (optional)
  ✔ metadata_field - 'metadata' field not present (optional)
  ✔ license_field - 'license' field not present (optional)
  ⚠ frontmatter_unknown_keys - Unknown frontmatter key(s) found; consider removing or moving to metadata
  ✔ body_present - SKILL.md body is present

Overall: PASSED (0 errors, 1 warnings)

Judge Evaluation

  Description: 100%
    specificity: 3/3 - Lists multiple specific concrete actions: 'Analyzes diffs or change descriptions, selects the correct type/scope, writes an imperative-mood header under 72 characters, and composes optional body and footer sections including breaking-change notation.'
    trigger_term_quality: 3/3 - Excellent coverage of natural terms users would say: 'commit messages', 'git commits', 'commit msg', 'staged changes', 'changelog entry', 'conventional commits', 'semantic versioning', 'version bumps'. These are all terms users would naturally use.
    completeness: 3/3 - Clearly answers both what ('Generates and formats git commit messages following the Conventional Commits specification') and when ('Use when the user asks for help writing commit messages, formatting git commits...') with explicit trigger guidance.
    distinctiveness_conflict_risk: 3/3 - Clear niche focused specifically on Conventional Commits specification with distinct triggers like 'conventional commits', 'semantic versioning', and specific formatting details (72 characters, imperative mood). Unlikely to conflict with general git or documentation skills.

    Assessment: This is an excellent skill description that hits all the marks. It provides specific concrete actions, comprehensive trigger terms that users would naturally say, explicit 'Use when' guidance, and a clear distinctive niche around Conventional Commits. The description uses proper third-person voice throughout.

  Content: 100%
    conciseness: 3/3 - Every section earns its place with no unnecessary explanation of concepts Claude already knows. The format is lean with tables, examples, and quick references that maximize information density.
    actionability: 3/3 - Provides concrete, copy-paste ready examples covering multiple scenarios (simple feature, bug fix with context, breaking change, performance improvement). The format template and type reference table give specific, executable guidance.
    workflow_clarity: 3/3 - For a single-task skill (writing commit messages), the workflow is unambiguous: format template → type selection → examples. The rules are explicit and the type reference table provides clear decision criteria.
    progressive_disclosure: 3/3 - Well-organized with clear sections progressing from format to types to examples to quick reference. External resources are appropriately linked at the end without nested references, keeping the main content self-contained.

    Assessment: This is an excellent skill that demonstrates strong content quality across all dimensions. It's concise yet comprehensive, with concrete examples covering various commit scenarios. The structure flows logically from format specification to type reference to practical examples, making it immediately actionable for Claude.

Average Score: 100%

✔ Skill evaluation completed successfully!
