{
  "checklist": [
    {
      "id": "c1",
      "criterion": "Uses ! for shell command injection: Uses `` !`git log --oneline -5` `` or similar for dynamic git context",
      "max_score": 25
    },
    {
      "id": "c2",
      "criterion": "Agent does NOT call /deploy recursively: Instructions do not tell the agent to invoke `/deploy` or other slash commands",
      "max_score": 25
    },
    {
      "id": "c3",
      "criterion": "Uses $ARGUMENTS or $1 for environment: Template uses `$ARGUMENTS` or `$1` to insert the environment name",
      "max_score": 20
    },
    {
      "id": "c4",
      "criterion": "Correct file location: Command file is placed in `.opencode/command/deploy.md`",
      "max_score": 15
    },
    {
      "id": "c5",
      "criterion": "Description frontmatter present: Has a YAML frontmatter block with a `description` field",
      "max_score": 15
    }
  ]
}
