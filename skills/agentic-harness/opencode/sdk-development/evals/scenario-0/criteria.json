{
  "context": "User wants a custom tool that searches for TODO comments.",
  "type": "weighted_checklist",
  "checklist": [
    {
      "name": "Correct import from @opencode-ai/plugin",
      "description": "Imports `tool` from `@opencode-ai/plugin`, NOT from `@opencode-ai/sdk`",
      "max_score": 20
    },
    {
      "name": "Correct file location",
      "description": "File placed in `.opencode/tool/` directory",
      "max_score": 15
    },
    {
      "name": "All schema args have .describe()",
      "description": "Both `directory` and `pattern` arguments include `.describe()` calls",
      "max_score": 25
    },
    {
      "name": "Execute returns a string",
      "description": "The execute handler returns a string (not an object or Promise<object>)",
      "max_score": 25
    },
    {
      "name": "Optional args with defaults",
      "description": "Uses `.optional()` or `.default(...)` for the optional arguments",
      "max_score": 15
    }
  ]
}
