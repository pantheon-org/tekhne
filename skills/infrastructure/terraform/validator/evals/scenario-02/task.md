# Scenario: Security scan with an evidence-cited report

A configuration defines an `aws_security_group` that allows SSH (port 22) from `0.0.0.0/0`. Run the security workflow and produce a findings report for this issue.
