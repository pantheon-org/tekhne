# Scenario: Generates a secure S3 bucket config and validates it before finishing

Generate an S3 bucket with versioning and server-side encryption. Split into main.tf/variables.tf/outputs.tf/versions.tf and validate.
