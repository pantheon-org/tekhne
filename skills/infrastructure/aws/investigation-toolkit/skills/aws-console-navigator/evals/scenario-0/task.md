# Expand a Log Row in CloudWatch Logs Insights via Playwright

## Task

You have a Playwright `page` object. The browser is on a CloudWatch Logs Insights page
showing a results table with 2 rows. You need to click on the first row to expand it and
reveal the full log event JSON.

Write the Playwright code to expand the first row.
