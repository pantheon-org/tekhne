# Scroll the CloudWatch Logs Results Table in Playwright

## Task

You have a Playwright `page` object. The browser is on a CloudWatch Logs Insights page.
A log row has been expanded but the full JSON content is below the visible area.

Write Playwright code to scroll down inside the results table to reveal the content.
Use a scroll offset of 600 pixels.
