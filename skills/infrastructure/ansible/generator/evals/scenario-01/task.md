# Scenario: Generates an idempotent playbook using purpose-built modules and handlers

Generate a playbook that installs nginx, deploys a config template, and restarts nginx only when the config changes.
