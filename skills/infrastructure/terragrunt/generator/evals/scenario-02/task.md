# Scenario: Generates a child module with a dependency block and mock_outputs

Generate a child terragrunt.hcl for an app module that depends on a network module, so plan works before the dependency is applied.
