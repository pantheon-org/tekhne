# Scenario: Generates a DRY root configuration with remote_state and generate blocks

Generate a root.hcl that centralises remote_state (S3 with locking) and a provider generate block for child modules to inherit.
