{
  "type": "weighted_checklist",
  "context": "Evaluates: Apply Adapter Pattern to Integrate an Incompatible External API",
  "checklist": [
    {
      "name": "LegacyLoggerAdapter implements ILogger: LegacyLoggerAdapter.ts declares 'implements",
      "description": "**LegacyLoggerAdapter implements ILogger**: `LegacyLoggerAdapter.ts` declares `'implements ILogger'` and has `info`, `warn`, and `error` methods",
      "max_score": 17
    },
    {
      "name": "info maps to LegacyLogger.log with INFO",
      "description": "**info maps to LegacyLogger.log with INFO level**: The `info` method calls `this.legacy.log('INFO', message, ...)` or equivalent",
      "max_score": 17
    },
    {
      "name": "warn maps to LegacyLogger.log with WARN",
      "description": "**warn maps to LegacyLogger.log with WARN level**: The `warn` method calls `this.legacy.log('WARN', message, ...)` or equivalent",
      "max_score": 17
    },
    {
      "name": "error maps to LegacyLogger.logError: The error",
      "description": "**error maps to LegacyLogger.logError**: The `error` method calls `this.legacy.logError(message, error, ...)` rather than `this.legacy.log` with ERROR level",
      "max_score": 17
    },
    {
      "name": "LegacyLogger accepted via constructor: LegacyLoggerAdapter accepts",
      "description": "**LegacyLogger accepted via constructor**: `LegacyLoggerAdapter` accepts a `LegacyLogger` instance as a constructor parameter (not instantiated inside the adapter)",
      "max_score": 16
    },
    {
      "name": "adapter-analysis.md correctly explains the pattern choice:",
      "description": "**adapter-analysis.md correctly explains the pattern choice**: `adapter-analysis.md` identifies the interface mismatch as the problem and explains the adapter preserves domain interface semantics without changing behavior",
      "max_score": 16
    }
  ]
}
