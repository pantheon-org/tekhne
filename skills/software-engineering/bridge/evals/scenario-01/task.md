# Task: Capture a Knowledge Bridge

The user issues the command:

```
/bridge HP → DS: the observability framework we built for the dashboard is reusable as atelier content
```

Load the project config, resolve aliases, auto-detect archetype from description keywords, determine direction and strength, write a YAML file to the bridges directory, and respond with a single confirmation line.
