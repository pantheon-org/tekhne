# Task: Capture a Bidirectional People-Bridge

The user issues the command:

```
/bridge HP ↔ SL: Matthieu carries context both ways between these two projects
```

Detect the `↔` bidirectional indicator, cross-reference "Matthieu" against config stakeholders for people-bridge detection, write a YAML file with `direction: bidirectional` and `archetype: people`, and respond with one line using `↔` notation.
