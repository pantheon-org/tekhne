# Task: Proactively Detect and Capture a Bridge from Natural Language

The user says (without an explicit /bridge command):

```
I just realised — the complexity-management patterns I'm developing for the regulatory compliance project would transfer perfectly to the financial-planning work. Same bureaucratic structure, same negotiation skills.
```

Recognise the cross-project bridge opportunity, resolve project references to config aliases via fuzzy matching, auto-detect archetype from keywords ("patterns", "transfer", "same skill", "bureaucracy") as `complexity`, write the YAML, and respond with one line before resuming the conversation.
