{
  "checklist": [
    {
      "id": "c1",
      "criterion": "Missing .PHONY identified: Agent identifies missing `.PHONY` and explains that a file named `clean` or `test` would silently block the target",
      "max_score": 12
    },
    {
      "id": "c2",
      "criterion": "Bare make identified: Agent identifies that `make -C packages/core all` uses bare `make` instead of `$(MAKE)` and explains the jobserver issue",
      "max_score": 11
    },
    {
      "id": "c3",
      "criterion": "Multi-line cd+command anti-pattern identified: Agent identifies that `cd dist` + `npm publish` on separate lines means the `cd` has no effect on the second line",
      "max_score": 11
    },
    {
      "id": "c4",
      "criterion": "Hardcoded variable assignment identified: Agent identifies that `CC`, `CFLAGS`, and `PREFIX` use `=` instead of `?=`",
      "max_score": 11
    },
    {
      "id": "c5",
      "criterion": "install uses cp instead of install command: Agent identifies that the `install` target uses `cp` instead of the `install` utility",
      "max_score": 11
    },
    {
      "id": "c6",
      "criterion": "Corrected Makefile uses .PHONY: The corrected Makefile declares all non-file targets in `.PHONY`",
      "max_score": 11
    },
    {
      "id": "c7",
      "criterion": "Corrected Makefile uses $(MAKE): The corrected Makefile replaces bare `make` with `$(MAKE)` for recursive calls",
      "max_score": 11
    },
    {
      "id": "c8",
      "criterion": "Corrected Makefile fixes cd+command: The corrected `deploy` target chains `cd` and the following command with `&&` or uses `.ONESHELL:`",
      "max_score": 11
    },
    {
      "id": "c9",
      "criterion": "Validation checklist confirmation: Agent lists which skill validation checklist items the corrected Makefile satisfies",
      "max_score": 11
    }
  ]
}
