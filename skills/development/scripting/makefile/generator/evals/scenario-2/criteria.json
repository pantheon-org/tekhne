{
  "checklist": [
    {
      "id": "c1",
      "criterion": "Modern header present: Makefile begins with the full modern header",
      "max_score": 15
    },
    {
      "id": "c2",
      "criterion": "PYTHON variable uses ?=: `PYTHON` is declared with `?= python3`",
      "max_score": 15
    },
    {
      "id": "c3",
      "criterion": ".PHONY covers all targets: `develop`, `test`, `lint`, `build`, `clean`, and `help` are all listed in `.PHONY`",
      "max_score": 14
    },
    {
      "id": "c4",
      "criterion": "develop target correctness: `develop` recipe installs in editable mode with dev extras using `$(PYTHON)` variable",
      "max_score": 14
    },
    {
      "id": "c5",
      "criterion": "clean target completeness: `clean` recipe removes `dist/`, `build/`, `*.egg-info/`, and `__pycache__` directories",
      "max_score": 14
    },
    {
      "id": "c6",
      "criterion": "Multi-step recipe safety: No recipe uses `cd` in one line and a command in the next separate line; commands are chained with `&&` or `.ONESHELL:` is active",
      "max_score": 14
    },
    {
      "id": "c7",
      "criterion": "help target with ## comment extraction: Each user-facing target has a `##` comment; `help` target extracts and prints them",
      "max_score": 14
    }
  ]
}
