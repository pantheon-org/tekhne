{
  "type": "weighted_checklist",
  "context": "Evaluates: Scenario 01: Nginx Access Log Analysis",
  "checklist": [
    {
      "name": "Line filter before parser",
      "description": "The 5xx log-filter query places a line filter (e.g. `|= \"50\"` or `|~ \"5[0-9]{2}\"`) BEFORE the parser stage \u2014 not after it",
      "max_score": 13
    },
    {
      "name": "Pattern parser used",
      "description": "Uses the pattern parser (`| pattern \"...\"`) to extract fields from the nginx access log format rather than regexp or json",
      "max_score": 13
    },
    {
      "name": "Specific stream selector",
      "description": "Stream selector includes at least two label matchers (e.g. job and namespace, or job and cluster) rather than a single broad label",
      "max_score": 13
    },
    {
      "name": "Status code via label filter",
      "description": "After parsing, the status code is filtered via a label filter (e.g. `status >= 500`) rather than only relying on the line filter",
      "max_score": 13
    },
    {
      "name": "rate() for metric query",
      "description": "The metric (rate) query uses `rate()` not `count_over_time()`",
      "max_score": 12
    },
    {
      "name": "sum by method grouping",
      "description": "The metric query aggregates using `sum by (method)` or equivalent grouping on the extracted HTTP method label",
      "max_score": 12
    },
    {
      "name": "Customization notes present",
      "description": "The output document includes notes identifying which labels or values to change (e.g. namespace, cluster, time window)",
      "max_score": 12
    },
    {
      "name": "Query explanations present",
      "description": "Each query is accompanied by a plain-English explanation of what it returns and how to interpret results",
      "max_score": 12
    }
  ]
}
