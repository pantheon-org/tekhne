# Scenario: Structure a raw procedure into an SOP

A user pastes an unstructured set of notes for deploying a service. Turn it into an
SOP with the standard anatomy (title, overview, prerequisites, numbered steps, verification).
