# Task: Debug disappearing page data

A developer says: "I created a custom `PageTitle` component and tried `const { title } = Astro.props` but title is undefined. What's wrong?"

Diagnose the issue and provide the correct code.
