# Scenario: Generates a Kubernetes log pipeline with metadata and bounded buffers

Generate a Fluent Bit config that tails container logs, enriches them with Kubernetes metadata, and ships them to Elasticsearch.
