{
  "context": "Agent must produce nx.json targetDefaults for build and test with the correct named inputs and cache settings as described by the nx-bun-integration skill.",
  "type": "weighted_checklist",
  "checklist": [
    {
      "name": "Schema and version present",
      "description": "nx.json has a $schema field and a version field (value 2).",
      "max_score": 10
    },
    {
      "name": "build targetDefault with cache: true",
      "description": "targetDefaults.build.cache is true.",
      "max_score": 20
    },
    {
      "name": "build inputs use production and ^production",
      "description": "targetDefaults.build.inputs contains both \"production\" and \"^production\".",
      "max_score": 20
    },
    {
      "name": "test targetDefault with cache: true",
      "description": "targetDefaults.test.cache is true.",
      "max_score": 20
    },
    {
      "name": "test inputs use default and ^production",
      "description": "targetDefaults.test.inputs contains both \"default\" and \"^production\".",
      "max_score": 30
    }
  ]
}
