{
  "context": "Agent must identify and fix all seven anti-patterns in the broken vite.config.ts, applying the correct patterns from the nx-vite-integration skill.",
  "type": "weighted_checklist",
  "checklist": [
    {
      "name": "nxViteTsPaths added to plugins",
      "description": "nxViteTsPaths is imported and called in the plugins array alongside react().",
      "max_score": 20
    },
    {
      "name": "root: __dirname and cacheDir added",
      "description": "root is set to __dirname and cacheDir is set to a workspace-root-relative path (../../node_modules/.vite/libs/shared-lib).",
      "max_score": 15
    },
    {
      "name": "build.outDir corrected to workspace-relative path",
      "description": "build.outDir is ../../dist/libs/shared-lib (not ./dist).",
      "max_score": 15
    },
    {
      "name": "test.cache.dir corrected to workspace-relative path",
      "description": "test.cache.dir is ../../node_modules/.vitest/libs/shared-lib (not ./node_modules/.vitest).",
      "max_score": 15
    },
    {
      "name": "test.coverage.reportsDirectory corrected to workspace-relative path",
      "description": "test.coverage.reportsDirectory is ../../coverage/libs/shared-lib (not ./coverage).",
      "max_score": 15
    },
    {
      "name": "rollupOptions.external added for React",
      "description": "build.rollupOptions.external includes 'react' and 'react-dom' at minimum.",
      "max_score": 20
    }
  ]
}
